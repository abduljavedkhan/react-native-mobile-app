class CartItem {
  constructor(itemImage, quantity, itemPrice, itemTitle, sum) {
    this.itemImage = itemImage;
    this.quantity = quantity;
    this.itemPrice = itemPrice;
    this.itemTitle = itemTitle;
    this.sum = sum;
  }
}

export default CartItem;
